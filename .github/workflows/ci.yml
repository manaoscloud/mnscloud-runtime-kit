name: ci

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  shell-syntax:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Validate shell syntax
        run: bash -n scripts/*.sh lib/*.sh installers/*.sh

